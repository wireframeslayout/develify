[memory_usage]
disabled = false
threshold = -1
symbol = " "
style = "bold green"

[username]
format = "[$user]($style)"
disabled = false
show_always = true

[hostname]
ssh_only = false
format = "[$ssh_symbol](bold blue) on [$hostname](bold red) "
trim_at = ".companyname.com"
disabled = false